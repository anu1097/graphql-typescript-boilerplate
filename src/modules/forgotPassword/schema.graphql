# import Error from "../../shared.graphql"

type Query{
  dummy: String
}

type Mutation {
  sendForgotPasswordLink(email: String!): Boolean
  setNewPassword(newPassword: String!, key: String!): [Error!]
}